{
  "name": "dtsr",
  "version": "0.1.7",
  "main": "dist/index.js",
  "license": "MIT",
  "bin": {
    "dtsr": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "description": "Type-level TypeScript runner",
  "scripts": {
    "build": "bun build index.ts --packages=external --outdir=dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/betafcc/dtsr.git"
  }
}